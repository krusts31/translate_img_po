{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "test-ci": "vitest --run",
    "coverage-ci": "vitest --run run --coverage"
  },
  "dependencies": {
    "jsdom": "^22.1.0"
  },
  "devDependencies": {
    "vite": "^4.3.0",
    "vitest": "^0.33.0"
  }
}
